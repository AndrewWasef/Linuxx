02) What is Jenkins agent? What is Jenkins executor (Build Executor)?
A jenkins agent is typically a machine, or container, which connects to a Jenkins controller and executes tasks when directed by the controller
A Jenkins executor is one of the basic building blocks which allow a build to run on a node/agent
